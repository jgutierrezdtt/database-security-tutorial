# Database Security

## Paso 3 de 10: Forzar SSL en tránsito

Crea `scripts/ssl-enforce.sql` que configure `require_secure_transport=ON` y deshabilite TLS 1.0/1.1.

### Tu tarea

Crea `scripts/ssl-enforce.sql` que configure `require_secure_transport=ON` y deshabilite TLS 1.0/1.1.

---
*Paso 3 de 10 — completa la tarea y haz commit para continuar.*
