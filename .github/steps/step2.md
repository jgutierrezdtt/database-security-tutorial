# Database Security

## Paso 2 de 10: RDS cifrada con KMS

Crea `iac/rds-encrypted.tf` con `aws_db_instance` con `storage_encrypted=true` y KMS key.

### Tu tarea

Crea `iac/rds-encrypted.tf` con `aws_db_instance` con `storage_encrypted=true` y KMS key.

---
*Paso 2 de 10 — completa la tarea y haz commit para continuar.*
