# Database Security

## Paso 7 de 10: Linting SQL en CI

Crea `.github/workflows/sqlfluff.yml` que ejecute sqlfluff con reglas de seguridad sobre migraciones.

### Tu tarea

Crea `.github/workflows/sqlfluff.yml` que ejecute sqlfluff con reglas de seguridad sobre migraciones.

---
*Paso 7 de 10 — completa la tarea y haz commit para continuar.*
